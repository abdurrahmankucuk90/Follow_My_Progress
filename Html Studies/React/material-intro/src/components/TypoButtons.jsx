import { Typography } from "@mui/material";

const TypoButtons = () => {
  return (
    <div>
      <Typography>Typography, Buttons, Container, Box</Typography>
    </div>
  );
};

export default TypoButtons;
